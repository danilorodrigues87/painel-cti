-- LEGADO — OpenClaw / Agent API HTTP removidos do produto.
-- O Assistente usa o bot Telegram nativo (agent_escola_config + telegram_agent_*).
-- Se estas tabelas existirem e não forem mais usadas, pode descartar:

-- DROP TABLE IF EXISTS `agent_api_audit`;
-- DROP TABLE IF EXISTS `agent_api_keys`;
